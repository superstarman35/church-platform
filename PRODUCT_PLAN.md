# 교회 전도지·모바일 초대장 우선 플랫폼 기획서

## 1. 서비스 정의

작은 교회와 단체가 부족한 인력으로도 모바일 전도지·초대장을 만들고 공유하며 참석 신청을 관리할 수 있도록 돕는 서비스다. 초대장 전용 MVP를 먼저 출시하고 안정화한 뒤 교회 홈페이지와 온라인 주보 기능으로 확장한다.

첫 번째 핵심 가치는 **교회 담당자가 복잡한 디자인 도구 없이 모바일 초대장을 만들고 링크와 QR로 공유하며 신청자를 관리하는 것**이다.

장기적으로는 한 번 입력한 교회 소식과 행사 정보를 홈페이지, 주보, 초대장, 일정 및 신청 페이지에서 함께 사용한다. 다만 이 통합 홈페이지 범위는 초대장 MVP 검증 이후에 개발한다.

> 교회 행사를 위한 모바일 초대장을 직접 만들고, 링크와 QR로 공유하며, 참석 신청까지 한곳에서 관리하는 운영 도구

## 2. 개발 우선순위와 첫 번째 버전의 범위

첫 번째 버전은 홈페이지가 없는 교회와 단체도 사용할 수 있는 전도지·초대장 전용 서비스다. 공통 인증, `church_id` 격리, 구독, 파일, 트래픽과 백업 기반은 초대장 기능을 안전하게 운영하기 위해 먼저 구축한다.

초대장 전용 MVP의 생성·공개·공유·신청·통계·사용량·백업 검증이 끝나기 전에는 교회 홈페이지, 홈페이지 메뉴·페이지·게시판·회원·설교와 온라인 주보를 개발하지 않는다. 아래 홈페이지와 온라인 주보 명세는 후속 개발 범위를 보존하기 위한 기획이다.

### 2.1 교회 개설

- 교회명과 로고
- 대표 이미지와 대표 색상
- 교회 소개
- 예배 시간
- 담임목사 및 섬기는 사람들
- 주소, 연락처 및 주차 안내
- 유튜브 채널
- 교회별 기본 서브도메인 자동 생성

### 2.2 후속 개발: 교회 홈페이지

초기에는 다음 3개의 검증된 템플릿을 제공한다.

- 따뜻하고 친근한 디자인
- 단정하고 전통적인 디자인
- 청년·다음 세대 중심 디자인

위 3개는 색상, 글꼴, 이미지 및 장식 방식이 다른 디자인 테마로 관리한다. 로고와 메뉴 위치, 대표 이미지 및 본문 섹션 배치가 다른 홈페이지 구조형 레이아웃은 별도로 제공한다. 교회는 `레이아웃 + 디자인 테마 + 색상 프리셋`을 조합해 홈페이지를 구성한다.

기본 페이지는 다음과 같다.

- 메인 화면
- 교회 소개
- 예배 안내
- 설교
- 교회 소식
- 일정
- 오시는 길
- 새가족 안내

자유 편집기보다 사진과 문구만 교체하면 홈페이지가 완성되는 방식을 우선한다.

### 2.3 후속 개발: 온라인 주보

- 지난주 주보 복제
- 예배 순서 작성
- 등록된 교회 소식과 행사 자동 불러오기
- 봉사 담당자 입력
- 모바일 주보 링크 생성
- 인쇄용 PDF 생성
- QR 코드 생성
- 지난 주보 보관

### 2.4 첫 MVP: 모바일 초대장

초기 제공 유형은 다음과 같다.

- 일반 예배
- 새생명축제
- 부활절
- 성탄절
- 창립기념예배
- 임직·취임예배

초대장에는 다음 기능을 제공한다.

- 행사 제목과 초대 문구
- 대표 이미지
- 일시와 장소
- 지도 및 길 찾기
- 교회 소개
- 참석 신청
- 문의하기
- 카카오톡 공유
- QR 코드

### 2.5 첫 MVP: 초대장 참석 신청

하나의 신청 기능을 다음 용도로 활용한다.

- 새가족 등록
- 예배 참석 신청
- 행사 신청
- 봉사 신청
- 기도 제목 접수

신청 상태는 `신규 → 확인함 → 연락 완료 → 처리 완료`로 관리하며, 엑셀 다운로드와 개인정보 삭제 기능을 제공한다.

### 2.6 첫 MVP: 초대장 전용 관리자

관리자 메뉴는 다음으로 단순하게 구성한다.

- 홈
- 교회 정보
- 초대장
- 신청 관리
- 사진·디자인 템플릿
- 방문·공유 통계
- 트래픽·저장공간
- 구독·결제
- 관리자 관리
- 고객지원
- 설정

관리자 홈에는 공개 중인 초대장, 신규 신청, 최근 조회·공유 현황, 트래픽·저장공간과 구독 상태를 표시한다. 초대장 전용 고객에게 홈페이지·메뉴·게시판·회원·설교 관리 메뉴를 노출하지 않는다.

### 2.7 사용자 권한

| 권한 | 범위 |
|---|---|
| 대표 관리자 | 모든 설정, 회원 및 결제 관리 |
| 편집자 | 허용된 초대장, 사진 및 신청 관리 |
| 열람자 | 신청 내역과 통계 확인 |

개인정보가 포함된 신청 내역은 별도 권한을 가진 사용자만 열람할 수 있게 한다.

## 3. 요금제 및 운영 모델

현재 구현·판매 기준은 홈페이지가 없는 교회와 단체를 위한 초대장 전용 상품이다. 홈페이지 상품의 가격과 세부 한도는 초대장 MVP 안정화 후 별도로 확정하며 현재 코드와 판매 화면에 적용하지 않는다.

### 3.1 초대장 30일 무료체험: 0원

- 월 트래픽 2GB
- 저장공간 200MB
- 활성 초대장 2개, 월 생성 2개
- 관리자 1명
- 사진 초대장당 5장
- 신청자 초대장당 50명

### 3.2 초대장 기본형: 월 4,900원

- 월 트래픽 10GB
- 저장공간 500MB
- 활성 초대장 5개, 월 생성 10개
- 관리자 2명
- 사진 초대장당 15장
- 신청자 월 500명
- 통계 보관 6개월

### 3.3 초대장 성장형: 월 9,900원

- 월 트래픽 30GB
- 저장공간 1.5GB
- 활성 초대장 20개, 월 생성 50개
- 관리자 5명
- 사진 초대장당 30장
- 신청자 월 3,000명
- 통계 보관 12개월

가격의 부가세 포함 여부, 연간 결제와 부가 옵션 가격은 판매 전에 별도로 확정한다. 광고 수익 모델은 적용하지 않는다.

## 4. 후원 모델

### 한 교회 세우기

후원 교회 또는 개인이 작은 교회의 초대장 전용 이용료를 대신 납부할 수 있다.

- 후원 상품과 기간은 운영자가 확인한 뒤 연결
- 후원받는 교회는 연결된 초대장 전용 상품 사용
- 후원자는 결제와 지원 현황만 확인
- 교회 운영 정보와 개인정보는 후원자에게 공유하지 않음
- 초대장의 후원 표기 여부는 교회가 선택
- 기본 후원 기간은 1년
- 후원 종료 30일 전 양측에 안내
- 후원이 종료되면 데이터를 유지하면서 초대장 무료체험 종료 정책 적용

후원 대상 교회는 공개 게시판에서 자동 연결하지 않고, 운영자가 소형·미자립교회 여부와 신청 내용을 확인한 뒤 연결한다.

## 5. 교회별 트래픽 및 사용량 정책

### 5.1 측정 항목

- 월간 페이지 조회 수
- 월간 순 방문자 수
- 이미지와 PDF 등의 전송량
- 저장 공간
- 초대장 및 주보 조회 수
- 신청 폼 제출 횟수
- 관리자 및 API 요청 횟수

동영상은 직접 보관하지 않고 유튜브 연결을 우선한다.

### 5.2 초기 권장 한도

| 항목 | 30일 무료체험 | 초대장 기본형 | 초대장 성장형 |
|---|---:|---:|---:|
| 월 전송량 | 2GB | 10GB | 30GB |
| 저장 공간 | 200MB | 500MB | 1.5GB |
| 활성 초대장 | 2개 | 5개 | 20개 |
| 월 생성 | 2개 | 10개 | 50개 |
| 신청 접수 | 초대장당 50명 | 월 500명 | 월 3,000명 |
| 관리자 수 | 1명 | 2명 | 5명 |

### 5.3 초과 정책

- 70% 사용: 관리자 화면 안내
- 85% 사용: 경고와 추가 구매·상향 안내
- 100% 사용: 신규 대용량 업로드 우선 제한
- 지속 초과: 다운로드·고용량 이미지 제한 후 운영자 확인
- 악성 트래픽: 요금제와 관계없이 속도 제한 또는 차단

정상적인 절기 행사 방문 증가로 홈페이지가 갑자기 차단되지 않게 한다. 검색엔진, 봇 공격, 반복 새로고침 및 외부 핫링크를 정상 과금 트래픽과 구분하고 CDN과 방화벽에서 먼저 처리한다.

각 요청과 저장 파일 및 폼 제출 기록에는 `church_id`를 포함해 교회별 사용량을 집계한다.

## 6. 도메인 정책

### 6.1 기본 구조

서비스 대표 도메인을 `service.kr`이라고 가정하면 다음 주소 체계를 제공한다.

| 유형 | 예시 | 제공 대상 |
|---|---|---|
| 기본 서브도메인 | `grace.service.kr` | 모든 교회 |
| 자체 도메인 | `gracechurch.kr` | 기본형 이상 |
| 기존 도메인 연결 | `www.gracechurch.kr` | 기본형 이상 |
| 임시 하위 주소 | `service.kr/c/grace` | 중복 및 장애 대응용 |

모든 교회에 기본 서브도메인을 먼저 발급하고, 원하는 교회만 자체 도메인을 연결한다.

### 6.2 기본 서브도메인

DNS는 와일드카드 레코드로 구성한다.

```dns
*.service.kr → 서비스 서버 또는 CDN
```

요청의 호스트명으로 교회를 식별해 해당 홈페이지를 제공한다.

서브도메인 규칙은 다음과 같다.

- 영문 소문자, 숫자 및 하이픈만 허용
- 3~30자
- 중복 사용 금지
- `admin`, `api`, `www`, `help` 등의 예약어 사용 금지
- 주소 변경 시 이전 주소를 일정 기간 새 주소로 이동
- 교회 탈퇴 후 6~12개월 동안 주소 재사용 방지

### 6.3 기존 자체 도메인 연결

일반적으로 교회가 다음 DNS 레코드를 설정하게 한다.

```dns
www.gracechurch.kr  CNAME  domains.service.kr
```

루트 도메인은 `www` 주소로 이동시키거나 DNS 업체의 ALIAS, ANAME 또는 CNAME Flattening 기능을 사용한다.

연결 절차는 다음과 같다.

1. 관리자가 사용할 도메인을 입력한다.
2. 서비스가 필요한 DNS 설정값과 소유권 확인값을 표시한다.
3. 교회가 도메인 업체에서 DNS를 변경한다.
4. 서비스가 소유권과 DNS 연결을 자동 확인한다.
5. SSL 인증서를 자동 발급한다.
6. 자체 도메인을 대표 주소로 설정한다.
7. 기존 서브도메인은 대표 도메인으로 이동시킨다.

### 6.4 신규 도메인 구매

도메인은 원칙적으로 교회 또는 교회 대표자의 명의로 직접 구매한다. 서비스는 연결 설정을 지원한다.

지원형에는 희망 도메인 검색, 교회 명의 등록, DNS 연결, 갱신 안내 및 관리자 정보 변경을 지원할 수 있다. 도메인 등록비와 관리 수수료는 구독료와 분리한다.

서비스 운영사 명의로 교회 도메인을 소유하지 않는다.

### 6.5 도메인 보안 및 운영

- DNS 확인 전 자체 도메인 활성화 금지
- 모든 도메인에 HTTPS 자동 적용
- 만료되거나 연결이 끊긴 도메인 자동 감지
- 교회 삭제 또는 해지 시 도메인 연결 해제
- 관리자 변경 및 도메인 연결 기록 보관
- SSL 인증서 발급 요청 횟수 제한
- 대표 도메인 하나만 검색엔진에 노출하고 나머지 주소는 이동 처리
- 관리자 로그인은 `admin.service.kr`과 같은 서비스 공통 주소 사용

교회 하나에 여러 도메인을 연결할 수 있도록 `Church`와 `Domain`을 분리해 설계한다.

```text
Church
- id
- name
- default_subdomain

Domain
- id
- church_id
- hostname
- type: subdomain / custom
- status
- is_primary
- verification_token
- ssl_status
- verified_at
```

## 7. 개인정보 및 운영 원칙

- 필요한 정보만 최소 수집
- 신청 폼에 개인정보 수집 동의 표시
- 교회별 데이터 완전 분리
- 관리자 권한 및 접근 기록 관리
- 신청 정보 보관 기간 설정
- 개별 신청자 삭제 기능
- 엑셀 다운로드 권한 제한
- 기도 제목의 공개·비공개 구분
- 서비스 해지 시 데이터 내보내기 제공
- 정기 백업과 복원 정책 수립

## 8. 개발 순서

### 1단계: 초대장 공통 기반

- PHP 애플리케이션 구조와 MariaDB 마이그레이션
- 플랫폼·교회 관리자 로그인
- 교회·단체 개설과 관리자 권한
- `church_id` 데이터 격리
- 초대장 전용 30일 체험과 구독 한도

### 2단계: 초대장 생성과 공개

- 다중 초대장 생성·수정·복제·게시·종료
- 행사 유형과 세로형·정사각형·가로형 디자인
- 이미지·YouTube·지도·연락처
- 공개·비밀번호·비공개·만료
- 모바일 공개 주소

### 3단계: 신청·공유·통계

- 참석 신청과 개인정보 동의
- 신청자 상태·출석 관리와 CSV
- 공유 링크·이미지와 QR 코드
- 조회·공유·신청 통계

### 4단계: 이미지·사용량·체험 운영

- WebP·썸네일·이미지 용량 검증
- 트래픽·저장공간 집계와 단계별 제한
- 초대장·사진·신청자 구독 한도
- 체험 만료와 유료 전환
- 매일 백업, 최근 7일 보관과 복구 시험

### 5단계: 초대장 MVP 안정화

- 역할별 권한과 교회 간 데이터 격리 검증
- PC·태블릿·모바일 검수
- 파일 업로드·CSRF·XSS·SQL Injection 방어 검증
- 실제 사용량과 운영 부하 측정
- 체험에서 유료로 이전하는 절차와 롤백 검증

### 6단계: 교회 홈페이지 후속 개발

- 교회 기본정보·템플릿·사용자 홈
- 메뉴·페이지·게시판·회원·YouTube 설교
- 온라인 주보와 콘텐츠 통합
- 자체 도메인과 홈페이지 상품 전환

초대장 MVP 안정화 완료 전에는 6단계를 시작하지 않는다.

## 9. 첫 출시 성공 기준

초대장 전용 MVP 출시 후 3개월 목표는 다음과 같다.

- 실제 사용 교회·단체 20곳
- 모바일 초대장 누적 100개
- 초대장 기본형 또는 성장형 유료 고객 5곳
- 초대장 생성 완료율 70% 이상
- 가입 후 첫 초대장 게시까지 평균 30분 이내
- 참석 신청과 공유 기능을 반복 사용하는 고객 비율 측정

가장 중요한 지표는 단순 방문자 수가 아니라 교회·단체가 행사마다 초대장 생성·공유·신청자 관리를 반복해서 사용하는 비율이다.

## 10. 첫 버전에서 제외하는 기능

- 교적부 전체 기능
- 헌금 및 회계 관리
- 출석 체크
- 심방 비밀 기록
- 교회 전용 모바일 앱
- 자체 영상 스트리밍
- 문자 대량 발송
- 교회 간 장비 거래
- AI 설교 생성
- 교회 홈페이지와 홈페이지용 메뉴·페이지·게시판·회원·설교
- 온라인 주보와 PDF
- 복잡한 홈페이지 자유 편집기
- 온라인 헌금 및 결제

위 기능은 초대장 전용 MVP의 사용성과 운영 안정성을 검증한 뒤 후속 단계에서 추가한다. 홈페이지 관련 기존 설계는 폐기하지 않고 후속 개발 기준으로 유지한다.

## 11. 서버 인프라 및 분산 운영안

### 11.1 확정 기본 구성

초기 운영부터 메인 서비스와 교회별 공개 서비스를 분리한다.

```text
Cloudflare
- DNS, SSL, CDN, 정적 파일 캐시 및 비정상 트래픽 차단
        |
        +-- 메인 서버: 카페24 리눅스 VPS 비즈니스
        |   - 대표 홈페이지
        |   - 통합 관리자 및 인증
        |   - 요금제, 결제 및 후원
        |   - 공통 DB와 서버 배정 정보
        |
        +-- CHURCH-01: 카페24 리눅스 VPS 자이언트
        |   - 교회 1~20의 홈페이지, 주보, 초대장 및 신청 기능
        |   - 해당 교회들의 콘텐츠 DB와 파일
        |
        +-- CHURCH-02: 카페24 리눅스 VPS 자이언트
        |   - 교회 21~40
        |
        +-- 교회가 20개 증가할 때마다 자이언트 서버 추가
```

공개 교회 홈페이지 요청은 메인 서버를 경유하지 않고 Cloudflare에서 해당 교회 서버로 직접 연결한다. 메인 서버는 공통 관리와 인증을 담당하고, 각 교회 서버는 자신에게 배정된 교회의 공개 화면과 데이터를 담당한다.

### 11.2 메인 서버

카페24 리눅스 가상서버호스팅 비즈니스 상품을 기본으로 사용한다.

| 항목 | 사양 |
|---|---:|
| 월 사용료 | 16,000원, VAT 포함 |
| 최초 설치비 | 22,000원 |
| RAM | 2GB |
| SSD | 40GB |
| 월 트래픽 | 500GB |
| 공인 IP | 1개 |
| 보유 도메인 연결 | 10개 |
| 권장 OS | Ubuntu 22.04 |

메인 서버는 다음 기능을 담당한다.

- 서비스 대표 홈페이지
- 통합 관리자
- 공통 회원가입, 로그인 및 인증
- 교회 개설과 사용자 권한
- 요금제, 결제 및 후원 이용권
- 교회별 도메인과 서버 배정
- 공통 공지 및 서비스 설정
- 서버 상태와 교회별 사용량 요약
- 공통 DB

메인 DB에는 `users`, `churches`, `memberships`, `subscriptions`, `payments`, `domains`, `shard_assignments` 및 `server_status` 등의 공통 데이터만 저장한다. 교회 콘텐츠와 신청자 개인정보를 메인 DB에 중복 저장하지 않는다.

메인 서버에서는 PDF 생성, 대용량 이미지 변환 및 교회 파일 전송을 처리하지 않는다. RAM 2GB 내에서 운영할 수 있도록 DB 연결 수, 로그 보관량 및 백그라운드 작업 수를 제한한다.

### 11.3 교회 분산 서버

카페24 리눅스 가상서버호스팅 자이언트 상품을 사용하고 서버 한 대에 최대 20개 교회를 배치한다.

| 항목 | 사양 |
|---|---:|
| 월 사용료 | 33,000원, VAT 포함 |
| 서버 추가 시 설치비 | 22,000원 |
| RAM | 4GB |
| SSD | 100GB |
| 월 트래픽 | 1.2TB |
| 공인 IP | 1개 |
| 보유 도메인 연결 | 20개 |
| 권장 OS | Ubuntu 22.04 |
| 담당 교회 | 목표 18개, 최대 20개 |

교회 분산 서버는 다음 기능을 담당한다.

- 교회 홈페이지
- 온라인 주보와 QR 코드
- 모바일 초대장
- 소식, 행사 및 설교 정보
- 신청 폼과 신청자 관리
- 교회별 디자인 설정
- 교회별 방문 통계
- 해당 서버에 배정된 교회들의 콘텐츠 DB
- 이미지, 생성된 PDF 및 기타 교회 파일
- 제한된 PDF 및 이미지 생성 작업

서버 한 대에서 DB 엔진 하나와 공통 애플리케이션 인스턴스 1~2개만 실행한다. 교회마다 별도 DB 프로세스나 Docker 컨테이너를 생성하지 않는다. 데이터는 모든 주요 테이블의 `church_id`로 분리하며, 필요할 경우 동일 DB 엔진 안에서 교회별 스키마를 사용할 수 있다.

### 11.4 교회 서버 배정

신규 교회에는 `server_group`을 지정한다.

```text
Church
- id
- name
- default_subdomain
- primary_domain
- server_group
- deployment_version
- storage_usage
- monthly_bandwidth
- published_at

WebServer
- id
- name
- public_ip
- capacity: 20
- active_churches
- traffic_limit_gb: 1200
- traffic_used_gb
- status
- last_deployed_at
```

서버당 목표 운영 수는 18개, 최대 수는 20개로 한다. 교회 16개 등록 시 다음 서버 신청을 준비하고, 18개부터는 신규 교회를 새 서버에 우선 배정한다. 방문과 파일 사용량이 큰 교회는 단순 교회 수보다 높은 가중치를 적용한다.

### 11.5 서버 비용

| 교회 수 | 메인 서버 | 교회 서버 | 월 합계 |
|---|---:|---:|---:|
| 1~20개 | 16,000원 | 33,000원 × 1 | 49,000원 |
| 21~40개 | 16,000원 | 33,000원 × 2 | 82,000원 |
| 41~60개 | 16,000원 | 33,000원 × 3 | 115,000원 |
| 61~80개 | 16,000원 | 33,000원 × 4 | 148,000원 |
| 81~100개 | 16,000원 | 33,000원 × 5 | 181,000원 |

초기 설치비는 메인 서버와 첫 교회 서버를 합해 44,000원이다. 이후 자이언트 서버를 추가할 때마다 설치비 22,000원이 한 번 발생한다. 외부 백업, 도메인 등록, 메일, 문자 및 결제 수수료는 서버 비용과 별도로 계산한다.

### 11.6 저장 공간 정책

자이언트 서버의 SSD 100GB 중 OS, 애플리케이션, DB, 로그, 임시 파일 및 안전 여유 공간을 제외하면 교회 파일에 약 55~65GB를 사용할 수 있다. 교회 20개 기준으로 평균 약 2.8~3GB에 해당한다.

초기 운영 한도는 다음을 기준으로 한다.

| 요금제 | 권장 저장 한도 |
|---|---:|
| 무료 | 300MB |
| 기본형 | 2GB |
| 지원형 | 3GB |

- 업로드 이미지는 최대 크기를 제한하고 WebP 또는 AVIF로 변환한다.
- 대표 이미지는 300~500KB, 일반 이미지는 150~300KB를 권장한다.
- PDF는 가능하면 5MB 이하로 생성한다.
- 서버 저장 공간이 70%에 도달하면 운영자에게 알린다.
- 90%에 도달하면 원본 이미지와 오래된 파일을 외부 저장소로 이동한다.
- 설교 영상은 직접 저장하지 않고 유튜브 연결을 사용한다.

### 11.7 트래픽 운영

자이언트 서버 한 대의 월 트래픽 1.2TB를 교회 20개가 공유한다. 교회당 월 20GB를 모두 사용해도 총 400GB이므로 기본 제공량 안에 충분한 여유가 있다.

- Cloudflare에서 이미지, PDF, CSS, JavaScript 및 공개 HTML을 캐시한다.
- 관리자, 로그인, 신청 제출, 결제 및 개인정보 API는 캐시하지 않는다.
- 이미지 핫링크와 악성 봇을 차단한다.
- 서버 트래픽이 70%, 85%, 90%에 도달할 때 단계적으로 알림을 보낸다.
- 트래픽이 큰 교회는 여유가 있는 다른 서버로 이전한다.

### 11.8 배포와 장애 범위

모든 교회 서버는 동일한 애플리케이션 버전과 DB 스키마를 사용한다. 공통 빌드 결과를 서버별로 순차 배포하며, 배포 실패 시 이전 버전으로 복귀할 수 있어야 한다.

교회 서버 한 대에 장애가 발생하면 해당 서버에 배정된 최대 20개 교회만 영향을 받는다. 다른 교회 서버와 메인 관리 기능은 계속 운영한다. 메인 서버 장애 시에도 Cloudflare와 교회 서버에 캐시된 공개 홈페이지는 가능한 범위에서 계속 표시하고, 로그인, 콘텐츠 수정, 결제 및 신청 접수 기능은 복구될 때까지 제한될 수 있다.

### 11.9 백업 정책

카페24 VPS의 운영 데이터는 별도 백업이 자동 제공된다고 가정하지 않고 직접 백업한다.

- 메인 DB는 매일 전체 백업한다.
- 각 교회 서버 DB는 매일 전체 백업한다.
- 신청자 개인정보가 포함된 백업은 암호화한다.
- 서버 내부에는 최근 7일 백업만 임시 보관한다.
- 외부 저장소에는 일간 14~30일, 주간 8주 및 월간 6개월 백업을 보관한다.
- 월 1회 실제 복원 시험을 실시한다.
- 교회별 데이터 내보내기와 복원이 가능해야 한다.

### 11.10 운영 전 확인 사항

- 카페24 리눅스 VPS 상품의 재고와 신청 가능 여부
- CPU 할당 및 공유 방식
- 기본 서브도메인과 자체 도메인의 연결 개수 계산 방식
- 루트 도메인과 `www`가 각각 도메인 수에 포함되는지 여부
- Cloudflare DNS를 통한 직접 연결도 보유 도메인 제한에 포함되는지 여부
- 상품 상향 변경 시 재부팅과 데이터 이전 필요 여부
- 월 트래픽 초과 과금과 차단 정책
- VPS 장애 시 복구 절차와 예상 소요 시간

### 11.11 교회별 트래픽 측정과 요금제 제한

각 교회의 트래픽은 `church_id`를 기준으로 별도 측정한다. 같은 교회 서버에 배정된 20개 교회가 서버의 월 트래픽을 공유하더라도, 요금제 한도와 관리자 화면에는 각 교회의 사용량을 독립적으로 표시한다.

초기 요금제별 월 전송량은 다음을 기본값으로 한다.

| 요금제 | 교회당 월 트래픽 | 적용 방식 |
|---|---:|---|
| 무료 | 2GB | 기본 홈페이지와 제한된 초대장 |
| 기본형 | 20GB | 홈페이지, 주보, 초대장 및 신청 기능 |
| 지원형 | 40GB | 방문이 많거나 제작 지원을 받는 교회 |

요금제 한도는 코드에 고정하지 않고 DB의 요금제 정책으로 관리한다. 향후 특별 요금제, 후원 요금제 및 교회별 임시 증설을 적용할 수 있어야 한다.

```text
plan_limits
- id
- plan_code
- monthly_bandwidth_bytes
- storage_limit_bytes
- monthly_page_view_limit
- monthly_submission_limit
- active_invitation_limit
- effective_from
- effective_to

church_quota_overrides
- id
- church_id
- monthly_bandwidth_bytes
- storage_limit_bytes
- starts_at
- ends_at
- reason
- created_by
```

교회의 월별·일별 사용량은 다음 구조로 저장한다.

```text
church_usage_monthly
- id
- church_id
- usage_month
- plan_code_snapshot
- bandwidth_limit_bytes
- origin_bytes
- cdn_bytes
- media_bytes
- total_bandwidth_bytes
- page_views
- submissions
- storage_bytes
- updated_at

church_usage_daily
- id
- church_id
- usage_date
- origin_bytes
- cdn_bytes
- media_bytes
- total_bandwidth_bytes
- page_views
- unique_visitors
- submissions

traffic_import_batches
- id
- source: nginx/cdn/storage
- server_group_id
- period_start
- period_end
- status
- checksum
- imported_at
```

`BIGINT` 바이트 단위로 저장하고 화면에서 MB와 GB로 변환한다. 요금제가 월 중 변경되어도 분쟁이 없도록 월별 사용량 레코드에 당시 요금제와 한도를 스냅샷으로 보관한다.

트래픽 집계 범위는 다음과 같다.

- Nginx가 직접 전송한 HTML, API 응답 및 원본 파일의 전송량
- Cloudflare 등 CDN 캐시에서 방문자에게 전송한 이미지, PDF, CSS, JavaScript 및 공개 HTML 전송량
- 교회별 미디어 및 PDF 다운로드량
- 정상 방문과 검색엔진 및 악성 봇 트래픽의 구분
- 내부 상태 확인, 서버 간 API, 관리자 내부 요청은 과금 트래픽에서 제외

CDN 캐시에서 처리된 전송량은 원본 서버 로그에 나타나지 않으므로 CDN 분석 또는 로그 API를 통해 호스트명별 사용량을 가져와야 한다. 원본과 CDN 트래픽을 중복 집계하지 않도록 `source`, 집계 시간 구간 및 체크섬을 기록한다. 원시 접속 로그는 장기 보관하지 않고 5~15분 또는 1시간 단위로 집계한 뒤 일별·월별 통계로 압축한다.

한도 적용 단계는 다음과 같다.

| 사용률 | 처리 |
|---:|---|
| 70% | 교회 관리자 화면에 사용량 안내 |
| 90% | 관리자와 서비스 운영자에게 경고 알림 |
| 100% | 정상 행사 대응을 위한 일시 유예 및 초과 안내 |
| 120% | 원본 이미지 다운로드 제한과 이미지 최적화 강화 |
| 150% | 대용량 미디어 제한 또는 운영자 확인 후 임시 제한 |

교회 홈페이지 전체를 즉시 차단하지 않는다. 예배 안내, 오시는 길 및 기본 홈페이지는 가능한 범위에서 계속 제공하고, 대용량 원본 이미지와 PDF 다운로드부터 단계적으로 제한한다. 악성 봇, 반복 새로고침 및 외부 핫링크는 요금제 사용량과 별도로 차단하거나 정상 과금 사용량에서 제외할 수 있게 한다.

각 요청은 호스트명으로 교회를 식별한 뒤 `church_id`와 요금제 한도를 확인한다. 매 요청마다 DB를 직접 조회하지 않고 1~5분 동안 캐시된 한도 상태를 사용한다. 사용량 집계 작업이 지연되어도 홈페이지 요청이 실패하지 않도록 측정과 제한 처리는 비동기로 구성한다.

교회 관리자에는 다음 화면을 제공한다.

```text
통계
└─ 사용량
   ├─ 이번 달 트래픽 사용량과 한도
   ├─ 남은 트래픽
   ├─ 일별 사용량 그래프
   ├─ 홈페이지·초대장·주보별 사용량
   ├─ 이미지·PDF 등 파일별 사용량
   ├─ 저장 공간 사용량
   ├─ 신청 접수 사용량
   ├─ 예상 월말 사용량
   └─ 최근 경고 및 제한 상태
```

관리자 홈에는 `사용량 / 한도 / 사용률`을 진행 막대로 표시한다. 대표 관리자와 사용량 열람 권한이 있는 사용자만 상세 내역을 볼 수 있게 하며, 70%와 90% 도달 시 관리자 화면 알림과 이메일을 제공한다.

서비스 운영자 화면에서는 서버별 총 트래픽과 교회별 사용량을 함께 확인한다.

```text
서버 관리
└─ CHURCH-01
   ├─ 서버 전체: 620GB / 1.2TB
   ├─ A교회: 18GB / 20GB
   ├─ B교회: 4GB / 20GB
   ├─ C교회: 31GB / 40GB
   └─ 이상 사용량 및 초과 예상 교회
```

서버 전체 사용량이 70%에 도달하면 신규 교회 배정을 중단하고, 트래픽이 큰 교회를 여유 서버로 이전할 수 있도록 한다. 교회별 제한과 서버 전체 1.2TB 제한을 동시에 감시한다.

### 11.12 카페24 교회 서버 추가 설치 런북

교회 서버에 16개 교회가 등록되면 다음 자이언트 서버 신청을 준비하고, 18개에 도달하기 전에 설치와 검증을 완료한다. 신규 서버는 즉시 운영에 연결하지 않고 `provisioning → testing → active` 단계를 거친다.

#### 1단계: 서버 추가 전 준비

- 다음 서버 이름을 정한다. 예: `church-02`, `church-03`.
- 메인 DB의 `server_groups`에 상태가 `provisioning`인 레코드를 생성한다.
- 새 서버가 사용할 환경변수, API 키 및 백업 저장 경로를 준비한다.
- 기존 운영 서버의 Docker Compose 파일과 애플리케이션 버전을 확인한다.
- 신규 서버 설치 중에는 교회를 배정하지 않는다.

#### 2단계: 카페24 서버 신청

카페24 리눅스 가상서버호스팅에서 자이언트 상품을 신청한다.

```text
상품: 리눅스 VPS 자이언트
RAM: 4GB
SSD: 100GB
월 트래픽: 1.2TB
OS: Ubuntu 22.04
서버명: church-02와 같은 내부 운영명
```

설치 완료 후 나의서비스관리에서 공인 IP, root 접속정보 및 서버 시작 상태를 확인한다. 카페24 보안 설정 스크립트를 사용할 경우 일반 사용자에게 필요한 `curl`, `python` 등의 실행 권한이 제한될 수 있으므로 애플리케이션 계정과 배포 방식의 호환성을 먼저 검증한다. 자체 보안 자동화를 사용할 때는 기본 OS로 설치한 뒤 아래 절차로 강화한다.

#### 3단계: 최초 접속과 관리자 계정

- 최초 root 접속 후 OS 패키지를 업데이트한다.
- 운영 관리자 계정을 만들고 `sudo` 권한을 부여한다.
- 운영 관리자 SSH 공개키를 등록한다.
- 공개키 접속을 확인한 뒤 root 직접 로그인과 비밀번호 로그인을 차단한다.
- 서버 내부 시간대는 UTC로 두고 화면 표시만 `Asia/Seoul`로 변환한다.
- 서버 이름을 신청 시 정한 `church-02` 형식으로 설정한다.

SSH 설정을 변경하기 전 새로운 터미널에서 공개키 로그인이 되는지 반드시 확인한다. 확인하지 않은 상태에서 기존 root 세션을 종료하지 않는다.

#### 4단계: OS 보안과 기본 도구

다음 항목을 설치하고 설정한다.

- UFW 또는 동등한 호스트 방화벽
- fail2ban
- 자동 보안 업데이트
- chrony 또는 systemd-timesyncd
- logrotate
- curl, git, unzip 및 인증서 도구
- 2GB swap
- 디스크 사용량 경고

기본 방화벽 정책은 다음과 같다.

```text
22/tcp   관리자 고정 IP 또는 VPN만 허용
80/tcp   HTTP에서 HTTPS 이동 및 인증서 확인
443/tcp  공개 홈페이지와 서버 간 HTTPS API
3306/tcp 외부 공개 금지
```

MariaDB는 `127.0.0.1` 또는 내부 컨테이너 네트워크에서만 수신한다. 메인 서버와 교회 서버의 통신도 MariaDB 직접 접속이 아니라 HTTPS API를 사용한다.

#### 5단계: 실행환경 설치

모든 교회 서버가 동일한 환경을 사용하도록 Docker Engine과 Docker Compose를 기본 배포 방식으로 사용한다. 운영 이미지는 개발 PC에서 직접 만들지 않고 공통 빌드 절차에서 생성하고 버전 태그를 붙인다.

```text
church-nginx
church-php
church-mariadb
church-worker
```

- `church-nginx`: 공개 페이지, 정적 파일 및 PHP 요청 전달
- `church-php`: PHP-FPM과 Laravel 교회 애플리케이션
- `church-mariadb`: 해당 서버의 최대 20개 교회 DB
- `church-worker`: Laravel 큐와 필요 시 Python PDF·이미지 작업

Node.js는 React 자산을 빌드할 때만 사용하고 운영 웹 프로세스로 상주시킬 필요가 없으면 실행하지 않는다. Python 작업은 동시 실행 수를 1개로 시작한다.

권장 디렉터리 구조는 다음과 같다.

```text
/srv/church-platform/
├─ compose.yml
├─ .env
├─ releases/
├─ current/
├─ storage/
│  ├─ media/
│  ├─ pdf/
│  └─ temporary/
├─ mariadb/
└─ backups/
```

`.env`, DB 파일, 미디어 및 백업은 애플리케이션 이미지 안에 포함하지 않는다. `.env` 파일 권한은 운영 계정만 읽을 수 있게 제한하며 Git에 저장하지 않는다.

#### 6단계: 서버별 비밀정보 생성

서버마다 다음 값을 독립적으로 발급한다.

```text
SERVER_GROUP=church-02
APP_KEY=서버별 애플리케이션 암호화 키
DB_PASSWORD=서버별 MariaDB 비밀번호
MAIN_API_URL=https://api.service.kr
SERVER_API_KEY=메인 서버 통신용 키
SERVER_HMAC_SECRET=요청 서명용 비밀키
BACKUP_ENCRYPTION_KEY=외부 백업 암호화 키
```

운영 서버 간 API 키와 DB 비밀번호를 여러 서버에서 재사용하지 않는다. 비밀정보 원본은 서버 밖의 안전한 비밀번호 관리 장소에도 보관한다.

#### 7단계: DB와 애플리케이션 초기화

- MariaDB 데이터 볼륨을 생성한다.
- 문자 집합은 `utf8mb4`, 시간대 저장은 UTC로 통일한다.
- 애플리케이션 전용 DB 사용자에게 필요한 DB 권한만 부여한다.
- Laravel 마이그레이션을 실행한다.
- 공통 템플릿과 초기 코드 데이터를 시드한다.
- 저장 디렉터리 권한을 PHP-FPM과 작업 프로세스에 맞게 설정한다.
- Nginx, PHP-FPM, MariaDB 및 작업 큐 상태를 확인한다.

신규 교회 서버에는 메인 DB의 사용자·결제 데이터를 복사하지 않는다. 교회 콘텐츠 스키마와 해당 서버에 배정된 교회 데이터만 저장한다.

#### 8단계: 메인 서버 등록

신규 교회 서버가 메인 서버의 등록 API에 다음 정보를 전송한다.

```text
server_group: church-02
public_ip
health_url
application_version
database_schema_version
storage_total_bytes
traffic_limit_bytes
capacity: 20
```

메인 서버는 서버 API 키와 HMAC 서명을 확인한 뒤 상태를 `testing`으로 변경한다. 서버 간 요청에는 시각, nonce 및 서명을 포함해 재전송 공격을 방지한다.

#### 9단계: 임시 도메인과 Cloudflare

운영 교회를 연결하기 전에 다음과 같은 내부 점검 주소를 사용한다.

```text
church-02-origin.service.kr
```

- Cloudflare DNS에 신규 서버 레코드를 등록한다.
- Origin 인증서 또는 공인 인증서를 설치한다.
- SSL 모드는 Full (strict)로 설정한다.
- HTTP를 HTTPS로 이동시킨다.
- 관리자·API 경로는 캐시하지 않는다.
- 이미지, PDF, CSS, JavaScript 및 공개 HTML 캐시를 확인한다.
- 원본 IP로 직접 접근할 때 올바르지 않은 호스트 요청을 거부한다.

#### 10단계: 기능 및 보안 검증

다음 항목이 모두 통과해야 서버를 운영 상태로 전환한다.

- 테스트 교회 생성과 삭제
- 템플릿 3종 표시
- 자체 도메인과 기본 서브도메인 연결
- 소식, 행사 및 설교 등록
- 복수 초대장 생성, 공개, 종료 및 복제
- 온라인 주보와 PDF 생성
- 신청 폼 제출과 개인정보 암호화
- 교회별 권한 분리
- 다른 `church_id` 데이터 접근 차단
- 이미지 업로드와 실행 파일 차단
- Cloudflare 캐시와 트래픽 집계
- 교회 관리자 사용량 화면
- 70%와 90% 트래픽 경고
- 메인 서버와 교회 서버 간 API 인증
- 서버 재부팅 후 자동 시작

#### 11단계: 백업과 복원 검증

- MariaDB 일별 백업 작업을 등록한다.
- 미디어와 PDF 증분 백업을 등록한다.
- 백업 파일을 암호화해 외부 저장소로 복제한다.
- 백업 성공·실패 알림을 메인 서버로 보낸다.
- 신규 서버 운영 전 테스트 교회 DB와 파일을 실제로 복원한다.
- 서버 내부 백업만 성공한 상태는 운영 준비 완료로 인정하지 않는다.

#### 12단계: 운영 전환

검증을 통과하면 메인 DB의 서버 상태를 `active`로 변경한다. 신규 교회 자동 배정 대상에 추가하되 처음에는 테스트 교회 또는 사용량이 적은 교회 1곳만 연결한다. 24시간 동안 오류율, PHP 메모리, DB 응답시간, 디스크 및 트래픽을 확인한 뒤 일반 배정을 시작한다.

#### 기존 교회를 신규 서버로 이전하는 순서

1. 이전 대상 교회와 작업 시간을 정한다.
2. 대상 교회를 짧은 읽기 전용 상태로 전환한다.
3. 기존 서버에서 해당 `church_id`의 DB 데이터를 내보낸다.
4. 미디어와 PDF 파일의 체크섬 목록을 생성한다.
5. 신규 서버로 DB와 파일을 복사한다.
6. 레코드 수, 파일 수, 총용량 및 체크섬을 비교한다.
7. 메인 DB의 `church_assignments`를 신규 서버로 변경한다.
8. Cloudflare 원본 연결을 신규 서버로 변경한다.
9. 홈페이지, 관리자, 신청 폼 및 초대장을 확인한다.
10. 읽기 전용 상태를 해제한다.
11. 기존 서버 데이터는 7일 동안 읽기 전용으로 보관한 뒤 백업 확인 후 제거한다.

DNS 전환 전에 신규 서버에서 도메인과 `church_id` 연결을 미리 검증한다. 이전 도중 실패하면 메인 서버 배정과 Cloudflare 연결을 기존 서버로 되돌린다.

#### 설치 완료 기준

다음 조건을 모두 만족해야 신규 서버 설치가 완료된 것으로 본다.

```text
[ ] SSH 공개키와 방화벽 설정 완료
[ ] root·비밀번호 SSH 로그인 차단
[ ] Nginx·PHP·MariaDB 정상
[ ] 애플리케이션과 DB 스키마 버전 일치
[ ] 메인 서버 등록 및 HMAC 통신 정상
[ ] Cloudflare SSL·캐시 정상
[ ] 교회 데이터 분리 테스트 통과
[ ] 트래픽 집계와 관리자 사용량 화면 정상
[ ] 외부 백업 성공
[ ] 실제 복원 테스트 성공
[ ] 재부팅 후 자동 복구 확인
[ ] 24시간 시험 운영 완료
```

### 11.13 서버 자동 설치 패키지 계획

수동 설치 런북이 여러 서버에서 검증된 후 카페24 자이언트 VPS를 자동으로 설정하는 설치 패키지를 제작한다. 자동화의 목표는 신규 서버를 신청한 뒤 운영자가 공인 IP와 일회용 등록 토큰만 입력하면 동일한 보안·애플리케이션·백업 구성이 재현되게 하는 것이다.

자동 설치는 다음 흐름을 사용한다.

```text
1. 메인 관리자에서 신규 서버 생성
2. server_group과 일회용 등록 토큰 발급
3. 카페24에서 Ubuntu 22.04 자이언트 VPS 신청
4. 신규 서버에서 설치 프로그램 실행
5. OS 보안, Docker, 애플리케이션 및 MariaDB 자동 구성
6. 메인 서버에 공개키와 서버 정보 등록
7. 자동 기능·보안·백업 테스트
8. testing 상태로 등록
9. 운영자 승인 후 active 전환
```

#### 자동 설치 패키지 구성

```text
server-installer/
├─ install.sh
├─ preflight.sh
├─ verify.sh
├─ rollback.sh
├─ ansible/
│  ├─ inventory.template.yml
│  ├─ site.yml
│  └─ roles/
│     ├─ base-os/
│     ├─ security/
│     ├─ docker/
│     ├─ church-app/
│     ├─ mariadb/
│     ├─ backup/
│     └─ monitoring/
├─ templates/
│  ├─ compose.yml
│  ├─ nginx.conf
│  ├─ php.ini
│  ├─ mariadb.cnf
│  ├─ supervisor.conf
│  └─ logrotate.conf
├─ checksums.txt
└─ VERSION
```

`install.sh`는 진입점으로 사용하고 실제 설정은 Ansible 역할과 버전이 지정된 템플릿으로 관리한다. 설치 과정은 여러 번 실행해도 같은 결과가 나오도록 멱등성 있게 작성한다.

#### 설치 실행 예시

```text
sudo ./install.sh \
  --server-group church-02 \
  --main-api https://api.service.kr \
  --registration-token 일회용토큰
```

등록 토큰은 짧은 유효시간과 1회 사용 제한을 적용한다. 명령행 기록에 토큰이 남지 않게 대화형 입력이나 토큰 파일 입력도 지원하고, 사용이 끝난 토큰은 즉시 폐기한다.

#### 자동 설정 범위

- OS 버전, RAM, 디스크 및 네트워크 사전 점검
- 운영 관리자 계정과 SSH 공개키 등록
- root·비밀번호 SSH 로그인 제한
- UFW, fail2ban 및 자동 보안 업데이트
- swap, 시간 동기화 및 로그 순환
- Docker Engine과 Docker Compose
- Nginx, PHP-FPM, Laravel 및 MariaDB 컨테이너
- 필요 시 Python 작업 환경과 PDF 생성기
- 서버별 환경변수와 암호화 키 생성
- DB 초기화, 마이그레이션 및 기본 데이터 입력
- 메인 서버 API 등록과 HMAC 비밀키 교환
- Cloudflare 연결에 필요한 DNS 값 출력
- 외부 백업과 실패 알림 등록
- 상태 확인, 트래픽 집계 및 모니터링 등록
- 재부팅 후 자동 시작 설정

#### 자동 설치 보안 원칙

- 설치파일에 운영 비밀번호, API 키 및 백업 키를 포함하지 않는다.
- 비밀정보는 서버에서 안전한 난수로 생성하거나 메인 서버와 1회성 보안 통신으로 전달한다.
- 배포 파일에는 버전, SHA-256 체크섬 및 서명을 제공한다.
- 인터넷에서 받은 스크립트를 검증 없이 root 권한으로 바로 실행하지 않는다.
- 설치 프로그램은 허용된 OS와 서버 사양에서만 실행한다.
- 실행 전 변경 예정 항목을 보여주는 `--check` 모드를 제공한다.
- 설치 로그에서 비밀번호와 토큰을 마스킹한다.
- 실패 시 설치 전 설정을 복구하거나 안전한 중단 상태로 남긴다.
- 자동 설치가 끝나도 운영자의 최종 승인 없이 교회를 배정하지 않는다.

#### 설치 상태 보고

설치 프로그램은 단계별 결과를 메인 관리자에 보고한다.

```text
신규 서버 church-02
├─ OS 점검: 완료
├─ 보안 설정: 완료
├─ Docker 설치: 완료
├─ 애플리케이션 배포: 완료
├─ DB 마이그레이션: 완료
├─ 메인 API 연결: 완료
├─ Cloudflare: 운영자 DNS 확인 필요
├─ 외부 백업: 완료
├─ 복원 테스트: 완료
└─ 상태: testing
```

실패한 단계부터 재실행할 수 있어야 하며, 설치 전체를 처음부터 다시 수행할 필요가 없게 한다.

#### 개발 단계

1. 첫 번째와 두 번째 교회 서버는 수동 런북으로 설치하며 모든 명령과 설정을 기록한다.
2. 반복되는 설치 작업을 `preflight.sh`, `install.sh` 및 `verify.sh`로 자동화한다.
3. 세 번째 서버부터 Ansible 역할과 공통 템플릿을 적용한다.
4. 백업 복원, 재부팅 및 실패 복구 테스트를 자동화한다.
5. 메인 관리자에서 설치 토큰 발급과 진행 상태 확인 화면을 제공한다.
6. 충분히 검증된 후 신규 자이언트 서버의 표준 설치 방식으로 지정한다.

자동 설치 패키지 자체도 애플리케이션과 별도로 버전 관리한다. 서버마다 설치된 인프라 버전을 `server_groups.infrastructure_version`에 기록하고, 오래된 서버는 순차적으로 업데이트한다.

### 11.14 교회 홈페이지 구조형 레이아웃

교회 홈페이지 레이아웃은 단순한 색상 차이가 아니라 로고, 내비게이션, 대표 이미지 및 주요 콘텐츠의 배치가 명확히 다르게 보이도록 구성한다. 초기에는 다음 4개를 제공한다.

#### 레이아웃 A: 왼쪽 로고형

```text
+----------------------------------------------------------+
| LOGO   교회소개  예배안내  설교  소식  주보  오시는 길 |
+----------------------------------------------------------+
|                    대표 이미지                          |
|                환영 문구와 바로가기                     |
+----------------------------------------------------------+
| 예배 안내 | 최근 설교 | 교회 소식 | 예정 행사           |
+----------------------------------------------------------+
```

- 로고는 왼쪽, 메뉴는 오른쪽에 배치한다.
- 가장 익숙하고 관리하기 쉬운 기본형이다.
- 일반 소형교회, 가족 중심 교회 및 정보 전달 중심 홈페이지에 적합하다.
- 무료 요금제의 기본 레이아웃으로 사용할 수 있다.

#### 레이아웃 B: 가운데 로고형

```text
+----------------------------------------------------------+
| 교회소개  예배안내     LOGO     설교  소식  오시는 길   |
+----------------------------------------------------------+
|                    대표 이미지                          |
|                  교회 표어·말씀                         |
+----------------------------------------------------------+
|         주보          행사          새가족 안내          |
+----------------------------------------------------------+
```

- 로고를 헤더 가운데 배치하고 메뉴를 좌우로 나눈다.
- 단정하고 전통적인 인상을 준다.
- 창립기념, 예전 중심 및 장년층 중심 교회에 적합하다.
- 메뉴 수가 많아지면 2단 메뉴 또는 더보기 메뉴를 사용한다.

#### 레이아웃 C: 대표 이미지 위 투명 헤더형

```text
+----------------------------------------------------------+
| LOGO       교회소개  예배  설교  소식  주보             |
|                                                          |
|                전체 화면 대표 이미지                    |
|              환영 문구 · 예배시간 버튼                  |
+----------------------------------------------------------+
| 최근 설교 | 이번 주 소식 | 초대장 | 오시는 길           |
+----------------------------------------------------------+
```

- 헤더를 대표 이미지 위에 겹쳐 표시한다.
- 스크롤하면 배경색이 있는 고정 헤더로 변경한다.
- 사진을 강조하는 교회, 청년부 및 다음 세대 중심 교회에 적합하다.
- 배경 이미지에 따라 로고와 메뉴의 명암 대비를 자동 또는 수동 설정한다.

#### 레이아웃 D: 상단 중앙 로고·하단 메뉴형

```text
+----------------------------------------------------------+
|                         LOGO                             |
|                 교회명 · 표어 · 연락처                  |
+----------------------------------------------------------+
| 교회소개 | 예배안내 | 설교 | 소식 | 주보 | 오시는 길   |
+----------------------------------------------------------+
| 대표 이미지 또는 이번 주 핵심 소식                     |
+----------------------------------------------------------+
| 예배 시간 | 새가족 안내 | 행사 초대장 | 지도            |
+----------------------------------------------------------+
```

- 첫 줄에 로고와 교회명을 크게 보여주고 메뉴는 아래 줄에 배치한다.
- 로고와 교회 정체성을 강조하고 메뉴가 많은 교회에 적합하다.
- 전통형과 따뜻한 디자인 테마에 모두 사용할 수 있다.

초기 4개 레이아웃을 운영한 뒤 실제 선택률과 변경 요청을 확인해 사진 중심형 또는 왼쪽 사이드 메뉴형을 추가할 수 있다. 첫 출시 단계에서는 구조형 레이아웃을 4개보다 과도하게 늘리지 않는다.

#### 디자인 테마와의 조합

구조형 레이아웃과 디자인 테마는 독립적으로 선택한다.

```text
구조형 레이아웃
- A: 왼쪽 로고형
- B: 가운데 로고형
- C: 투명 헤더형
- D: 상단 중앙 로고·하단 메뉴형

디자인 테마
- warm: 따뜻하고 친근한 디자인
- classic: 단정하고 전통적인 디자인
- next: 청년·다음 세대 중심 디자인
```

4개 레이아웃과 3개 테마를 조합해 최대 12개의 기본 인상을 제공하되, 품질이 검증된 조합만 관리자 미리보기에 노출한다.

#### 레이아웃 공통 편집 범위

교회 관리자는 다음 항목을 변경할 수 있다.

- 로고 이미지와 표시 크기
- 교회명 또는 로고만 표시
- 헤더 높이
- 메뉴 정렬과 순서
- 대표 이미지와 대표 문구
- 주 색상과 보조 색상
- 글꼴 프리셋
- 밝은 로고와 어두운 로고 선택
- 헤더 고정 여부
- 섹션 표시·숨김
- 섹션 순서
- 카드형 또는 목록형 콘텐츠 표시

관리자가 로고를 화면 밖으로 이동시키거나 메뉴 구조를 깨뜨리는 완전 자유 편집은 제공하지 않는다. 검증된 범위 안에서 선택하도록 한다.

#### 모바일 표시 원칙

데스크톱에서 로고 위치가 달라도 모바일에서는 각 레이아웃의 특징을 유지하면서 화면 폭에 맞게 단순화한다.

```text
왼쪽 로고형:   [LOGO]                        [메뉴]
가운데 로고형: [메뉴]        [LOGO]          [기능]
투명 헤더형:   대표 이미지 위 [LOGO]         [메뉴]
상단 중앙형:          [LOGO]
                  [메뉴 버튼]
```

- 모바일 메뉴는 햄버거 또는 전체 화면 메뉴로 제공한다.
- 로고가 너무 작아지지 않도록 최소·최대 크기를 제한한다.
- 전화, 지도, 주보 및 예배시간 바로가기를 하단 또는 고정 버튼으로 제공할 수 있다.
- 메뉴를 연 상태에서도 키보드 탐색, 초점 이동 및 닫기 기능을 지원한다.
- 모든 레이아웃은 같은 콘텐츠와 URL 구조를 사용한다.

#### 저장 구조

```text
church_design_settings
- church_id
- layout_code: header-left/header-center/header-overlay/header-stacked
- theme_code: warm/classic/next
- color_preset
- primary_color
- secondary_color
- font_preset
- logo_media_id
- alternate_logo_media_id
- logo_size
- header_height
- sticky_header
- section_order_json
- section_visibility_json
- updated_at
```

레이아웃 변경 시 콘텐츠는 삭제하지 않고 표시 구조만 변경한다. 변경 전 설정을 버전으로 보관해 이전 디자인으로 되돌릴 수 있게 한다.
